import { contactMessage } from '../../styles/style';
import { Container } from '../../Components';

const Contact: any = () => {
  return (
    <Container className={contactMessage}>
      <h1>{`Let's get in touch`}</h1>
    </Container>
  );
};

export { Contact };
